---
# Windows 専用インベントリ（統合インベントリ inventory/hosts.yml の使用を推奨）
#   ansible-playbook -i inventory/windows-hosts.yml playbooks/windows-client-setup.yml
all:
  children:
    windows_clients:
      hosts:
        win-target:
          ansible_host: 192.168.100.20
          ansible_user: cocoon
          ansible_password: "C@c#on160"
