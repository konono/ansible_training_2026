# Ansible用語集

---

## 1. 基本概念

| 用語 | 説明 |
|---|---|
| **Ansible** | エージェントレスで動作するIT自動化ツール。SSH経由で対象ホストを操作する。 |
| **コントローラー / コントロールノード** | Ansibleコマンドを実行する側のマシン。管理対象ノードにSSH接続して操作を行う。 |
| **管理対象ノード（マネージドノード）** | Ansibleから操作される側のホスト。 |
| **インベントリ（inventory）** | Ansibleが操作対象とするホストとグループを定義したファイル。どのホストに対して自動化処理を行うかを管理する、Ansibleの基本要素の一つ（詳細はセクション6を参照）。 |
| **モジュール（module）** | 「パッケージインストール」「サービス起動」など、よくあるIT運用作業を部品化したもの。Ansibleの操作はすべてモジュールを介して行われる。数千種類が組み込みで提供され、Linux/Windows/ネットワーク機器/クラウドまで幅広く対応する。 |
| **ad-hocコマンド** | `ansible` コマンドを使ってモジュールを1回限りで直接実行する方法。Playbookを書かずに動作確認や簡単な作業を行う際に使う。 |
| **playbook** | 複数のモジュール呼び出し（task）をYAML形式でまとめたファイル。自動化処理を体系的に記述・管理できる。 |
| **play** | playbook内の要素で、特定のグループ/ホストに対して実行する一連のtaskをまとめたもの。1つのplaybookに複数のplayを含められる。 |
| **task** | ad-hocコマンドと同様の、1つ1つのモジュール呼び出しの定義。playの中に複数定義する。 |
| **role** | playbookを構造化されたディレクトリ構成に分解し、再利用可能な部品として管理する仕組み。`tasks/` `handlers/` `templates/` `vars/` `defaults/` `meta/` などのディレクトリで構成される。 |
| **冪等性（idempotency）** | 同じ処理を何度実行しても結果が変わらない性質。Ansibleの多くのモジュールは、対象が既に目的の状態であれば何もしない（`changed: false`）ため、繰り返し実行しても安全。 |
| **facts（ファクト）** | 対象ホストのOS・ハードウェア・ネットワークなどに関する情報。`setup` モジュールが自動収集し、playbook内で変数として利用できる。play実行時、最初に暗黙的に `Gathering Facts` タスクが動く。 |
| **Ansible Galaxy** | コミュニティが作成したroleやcollectionを共有・検索できる公式プラットフォーム。`ansible-galaxy` コマンドでroleの雛形作成やダウンロード、collectionのインストールができる。 |
| **collection（コレクション）** | モジュール・プラグイン（filter/lookup/testなど）・role・playbookをまとめて配布するためのパッケージ形式。Ansible本体とは別にバージョン管理され、`ansible-galaxy collection install <名前>` でインストールする（例: `ansible.utils`、`community.general`）。Ansible 2.10以降、多くのモジュールが本体から分離してcollectionとして提供されるようになった。 |
| **ansible.builtin** | Ansible本体に同梱されている、最も基本的なモジュール・プラグイン群が属するcollection名（例: `package`、`service`、`copy` など）。 |
| **FQCN（完全修飾コレクション名 / Fully Qualified Collection Name）** | `ansible.utils.ipaddr` や `ansible.builtin.package` のように、`名前空間.コレクション名.モジュール名` の形式でモジュール/フィルタ/プラグインを一意に指定する記法。同名モジュールが複数collectionに存在する場合の曖昧さを避けるため、明示的な記述が推奨される。 |
| **requirements.yml** | 使用するcollectionやroleの一覧・バージョンを定義するファイル。`ansible-galaxy collection install -r requirements.yml` のようにまとめてインストールできる。 |
| **ansible-doc** | インストール済みのモジュール・プラグインのドキュメントをターミナル上で確認できるコマンド（例: `ansible-doc command`）。 |

---

## 2. 設定・実行系

| 用語 | 説明 |
|---|---|
| **ansible.cfg** | Ansibleの動作を制御する設定ファイル。カレントディレクトリ・ホームディレクトリ・システム全体などに配置可能で、検索順位により優先度が決まる（カレントディレクトリが最優先）。環境変数 `ANSIBLE_CONFIG` で明示的に指定するとさらに優先される。 |
| **become** | リモートホストで権限昇格（管理者権限での実行）を行う指定。ad-hocコマンドでは `-b`、playbookでは `become: yes` と記述する。デフォルトは `sudo`。 |
| **--check（チェックモード）** | 実際には変更を加えず、実行結果を事前にシミュレーションするドライラン機能。 |
| **--syntax-check** | playbookのYAML構文にエラーがないかを確認するオプション。 |
| **--diff** | `--check` と組み合わせ、変更前後の差分を表示するオプション。 |
| **-e（extra vars）** | playbook実行時にコマンドラインから変数を渡し、上書きするオプション。変数の優先順位の中で最も高い。 |
| **--limit** | インベントリ内の特定ホスト/グループのみに実行対象を絞り込むオプション。 |
| **--start-at-task** | 指定したタスクから実行を再開するオプション。 |
| **--step** | タスクごとに実行確認をしながらplaybookを進めるオプション。 |
| **verbose（-v 〜 -vvvv）** | 実行時の詳細ログの出力レベルを上げるオプション。段階が上がるほど詳細な情報（SSH接続の詳細など）が表示される。 |
| **PLAY RECAP** | playbook実行結果のサマリー。`ok` / `changed` / `unreachable` / `failed` / `skipped` / `rescued` / `ignored` の件数が表示される。 |

---

## 3. 変数関連

| 用語 | 説明 |
|---|---|
| **vars** | play内やrole内で使用する変数を定義するブロック。 |
| **スカラー変数** | 単一の値（文字列・数値など）を持つ変数。 |
| **リスト変数** | ハイフンで複数の値を並べて持つ変数。 |
| **変数展開** | `{{ 変数名 }}` の記法で変数の値を埋め込むこと。Jinja2テンプレートエンジンの仕様であり、YAML中では常にクオートで囲む必要がある。 |
| **defaults（role）** | role内で定義する、外部から上書き可能な低優先度のデフォルト変数（`defaults/main.yml`）。利用者が自由に変更することを想定している。 |
| **vars（role）** | role内で固定値として使う、優先度の高い変数（`vars/main.yml`）。通常は外部から上書きしない。 |
| **vars_files** | 外部のYAMLファイルから変数を読み込むplayレベルのキーワード。 |
| **include_vars** | タスクの実行時に動的に変数ファイルを読み込むモジュール。優先順位が非常に高く、task varsでは上書きできない点に注意。 |
| **set_fact** | タスク実行中に動的に変数（ファクト）を設定するモジュール。ホスト単位で保存され、play間・role間で値を共有する際にも使う。 |
| **register** | タスクの実行結果を変数に格納するキーワード。後続タスクの条件分岐などで利用する。 |
| **マジック変数（Magic Variables）** | `inventory_hostname` など、Ansibleが自動的に設定する特別な変数。 |
| **hostvars** | 他のホストの変数値へアクセスするための辞書（`hostvars[ホスト名][変数名]`）。 |
| **group_vars / host_vars** | インベントリのグループ単位・ホスト単位で変数を定義するディレクトリ／ファイル（例: `group_vars/web.yml`）。 |
| **変数の優先順位（Variable Precedence）** | Ansibleにおける変数の適用優先度のルール。低い方から「role defaults」「inventory変数」「playbook変数」「facts」「play vars」「role vars」「include_vars」「set_fact」「role params」「extra vars（最高）」といった約20段階が定義されている。 |
| **vars_prompt** | playbook実行時に対話的に変数値の入力を求める機能。 |

---

## 4. テンプレート（Jinja2）

| 用語 | 説明 |
|---|---|
| **Jinja2** | Ansibleが変数展開・テンプレート処理に使用するPython製テンプレートエンジン。 |
| **template モジュール** | Jinja2テンプレートファイル（拡張子 `.j2`）を変数展開したうえでリモートホストに配置するモジュール。`copy` モジュールと異なり動的な内容生成が可能。 |
| **{{ }}（変数展開）** | Jinja2で変数の値を出力する記法。 |
| **{% %}（制御構文）** | Jinja2の `for` ループや `if` 条件分岐などを記述する記法。 |
| **{# #}（コメント）** | Jinja2テンプレート内のコメント記法。 |
| **フィルタ（filter）** | 変数の値を加工するJinja2の機能（例: `default`、`json_query`、`regex_replace`、`combine`、`dict2items`、`ipaddr` など）。`|` で繋げて使用する。 |
| **テスト（test）** | 条件の真偽を判定するJinja2/Ansibleの機能（例: `is succeeded`、`is version`）。 |
| **lookupプラグイン** | 外部（ファイル・環境変数・コマンド実行結果など）から値を取得する仕組み（例: `lookup('file', ...)`、`lookup('pipe', ...)`、`lookup('env', ...)`）。 |
| **空白制御（{%- -%}）** | Jinja2の制御構文タグの前後にハイフンを付けることで、余分な空白・改行を除去する記法。Ansibleの `template` モジュールはデフォルトで `trim_blocks` / `lstrip_blocks` が有効。 |
| **テンプレートの継承** | `{% extends %}` や `{% block %}` を使い、ベーステンプレートを子テンプレートで拡張・上書きする仕組み。 |
| **サンドボックス** | Jinja2テンプレート内で実行可能な処理を制限し、安全性を高める仕組み。 |
| **Jinja2ネイティブ型** | テンプレート展開結果を文字列ではなく元のデータ型（数値・真偽値など）のまま保持する機能。 |

---

## 5. 制御構文・実行制御

| 用語 | 説明 |
|---|---|
| **loop** | taskを繰り返し実行するためのキーワード。ループ対象のリストを指定し、各要素は `item` という変数でtask内から参照できる（旧来の `with_items` に代わる推奨記法）。 |
| **when** | 条件に応じてtaskの実行可否を制御するキーワード。ファクトや `register` の結果などを条件式に使う。リストで複数条件を指定するとAND評価となる。 |
| **changed_when** | タスクの `changed` 判定結果を明示的に上書きする指定。設定確認など、システムを変更しないコマンドで `false` を指定することが多い。 |
| **failed_when** | タスクの失敗判定条件を独自に定義する指定。 |
| **ignore_errors** | タスクが失敗してもplaybookの実行を中断せず継続させる指定。 |
| **assert モジュール** | 指定した条件が満たされているかを検証するモジュール。バリデーション用途で使う。 |
| **block / rescue / always** | 複数taskをまとめて例外処理を行う構文。`block` は通常処理、`rescue` はエラー発生時の処理、`always` は成功・失敗に関わらず必ず実行される処理。 |
| **ハンドラ（handler）** | `notify` によって呼び出される特殊なtask。呼び出し元taskが `changed` になった場合のみ、playの最後に1回だけ実行される（例: 設定変更後のサービス再起動）。 |
| **notify** | taskの変更発生時にハンドラを呼び出すキーワード。 |
| **tags** | task/playに付与するラベル。`--tags` / `--skip-tags` で実行対象を選択的に制御できる。`always` `never` という特殊タグもある。 |
| **include_tasks / import_tasks** | 別ファイルに定義したtaskを読み込む仕組み。`include_tasks` は実行時に動的評価、`import_tasks` はplaybook解析時に静的評価される。 |
| **import_role** | role全体をtask内から呼び出すモジュール。role同士の依存関係の実装などに使う。 |
| **serial** | ローリングアップデートを行うための指定。対象ホストを指定数（またはパーセント）ずつ順番に処理する。 |
| **delegate_to** | taskの実行主体を、対象ホストではなく別のホスト（コントローラー等）に委任する指定。 |
| **run_once** | 対象ホストが複数あっても、taskを1回だけ実行する指定。`delegate_to` と組み合わせて使うことが多い。 |
| **until / retries / delay** | taskが成功するまで指定回数・間隔でリトライする制御構文。ヘルスチェック等に使う。 |
| **wait_for モジュール** | TCPポートの開放やファイルの出現などを待機するモジュール。 |
| **uri モジュール** | HTTP(S)リクエストを送信し、レスポンスを検証するモジュール。 |
| **check_mode** | タスク単位でチェックモードの適用有無を制御するパラメータ。 |
| **debugger** | playbook実行中にインタラクティブなデバッグセッションを起動する機能（`debugger: on_failed` など）。 |
| **debug モジュール** | 変数の値やメッセージを出力するモジュール。`var`（構造確認）と `msg`（人間向けメッセージ）の2種類の使い方がある。 |

---

## 6. インベントリ・ホスト管理

| 用語 | 説明 |
|---|---|
| **インベントリ（inventory）** | Ansibleの管理対象ホストと、それらが所属するグループを定義するファイル（または動的スクリプト）。デフォルトでは `/etc/ansible/hosts` が使われるが、`ansible.cfg` の `inventory` 設定や `-i` オプションで任意のパスを指定できる。INI形式とYAML形式があり、YAML形式は階層構造やリスト・辞書の変数を表現しやすい。 |
| **静的インベントリ** | あらかじめファイルにホスト・グループを記述しておく、最も基本的なインベントリの形式（本トレーニングの `inventory.yml` など）。 |
| **all** | 定義不要で使える特別なグループ名。インベントリに記載された全ホストを指す。 |
| **ungrouped** | どのグループにも属していないホストが自動的に所属する特別なグループ名。 |
| **グループ（group）** | インベントリ内でホストをまとめる単位（例: `web`、`loadbalancer`）。1台のホストが複数グループに所属することも可能。 |
| **子グループ（children）** | あるグループの中にさらにグループを定義する入れ子構造（例: `web` グループの中に `web_primary` を作るなど）。 |
| **インベントリ変数** | `all.vars` やグループ・ホスト単位で定義する変数。接続情報（`ansible_user` など）から任意のカスタム変数まで幅広く使う。 |
| **ansible_host** | インベントリでホストの実際の接続先アドレスを指定する変数。 |
| **ansible_user / ansible_ssh_pass / ansible_port** | SSH接続に使うユーザー名・パスワード・ポート番号を指定する接続用変数。 |
| **動的インベントリ / 動的グループ** | 実行時にホストやグループを動的に追加・分類する仕組み（`add_host`、`group_by` モジュールなど）。 |
| **ping モジュール** | 対象ホストがAnsibleから操作可能（SSH接続・Python利用が可能）であることを確認するモジュール。通常のICMP pingとは異なる。 |
| **複数環境インベントリ** | ステージング・本番など環境ごとにインベントリファイルを分けて管理する運用パターン。 |

---

## 7. セキュリティ

| 用語 | 説明 |
|---|---|
| **ansible-vault** | パスワードやAPIキーなどの秘密情報を暗号化して管理するAnsibleの機能。暗号化ファイルの作成・編集・復号を行うコマンド群を提供する。 |
| **vault password file** | `ansible-vault` の復号に使うパスワードを記載したファイル。バージョン管理に含めないよう注意が必要。 |
| **lineinfile モジュール** | ファイル内の特定行を追加・置換・削除するモジュール。冪等性を保ちながら1行単位の編集ができる。 |
| **blockinfile モジュール** | ファイル内の複数行のブロックをまとめて挿入・管理するモジュール。 |

---

## 8. 品質・ベストプラクティス

| 用語 | 説明 |
|---|---|
| **ansible-lint** | playbookやroleの記述が公式のベストプラクティスに沿っているかを静的解析するツール。 |
| **.ansible-lint** | ansible-lintの除外ルールやプロファイルなどを定義する設定ファイル。 |
| **ディレクトリレイアウト（ベストプラクティス）** | role単位でtasks/handlers/templates/vars/defaults/metaなどをディレクトリ分割して管理する、公式推奨の構成。 |
| **meta/main.yml（role依存関係）** | roleが依存する別のroleを定義するファイル。 |
| **CI/CDパイプラインでのlint実行** | GitHub Actions等でansible-lintを自動実行し、品質を継続的に担保する運用。 |

---

## 9. その他（Windows対応）

| 用語 | 説明 |
|---|---|
| **WinRM** | Windowsノードに対してAnsibleが接続する際に使うリモート管理プロトコル。 |
| **win_ping / win_service / win_package / win_chocolatey** | Windows専用モジュール群。`win_` プレフィックスが付き、Linux用モジュール（`service`、`package` など）とは別に用意されている。 |
| **Chocolatey** | Windows向けのパッケージ管理ツール。`win_chocolatey` モジュールから利用できる。 |

---

