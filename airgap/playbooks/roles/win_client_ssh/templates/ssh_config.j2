# Ansible 研修環境 — 自動生成（{{ ansible_date_time.date | default('') }}）
Host training
    HostName {{ rhel_target_ip }}
    Port {{ controller_ssh_port }}
    User {{ controller_user }}
    StrictHostKeyChecking no
    UserKnownHostsFile /dev/null
